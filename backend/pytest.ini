[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
    --cov=converter
    --cov=app
    --cov-report=term-missing
    --cov-report=html
